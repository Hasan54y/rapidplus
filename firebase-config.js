// Firebase web config for rapidplusbrowser.online sign-in.
// These values are public identifiers (they ship to every visitor's browser); access is controlled by the
// sign-in providers and Firestore rules, not by hiding this file.
// Fill them in from: Firebase console -> Project settings -> Your apps -> Web app -> SDK setup and configuration.
window.RAPID_FIREBASE = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  appId: "PASTE_APP_ID"
};
